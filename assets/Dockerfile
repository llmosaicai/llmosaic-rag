FROM alpine:3.20
WORKDIR /assets
COPY specs/ ./specs/
# Placeholder to also include example SQL and notebooks if desired
# They are already present in earlier image tags; this Dockerfile focuses on specs.

